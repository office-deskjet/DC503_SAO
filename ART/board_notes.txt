removed inside with gimp and saved as png.

opned in inkscape
Transform with scaling in mm. have to be in "select and transform object mode(F1)"

Make it into a vector
Path > Trace Bitmap > defualt.
OK then close window.
drag the image to show old image. delete old image

"Edit paths by nodes (F2)" tool drag select inner nodes. delete them.
Image becomes solid black.
under Fill and Stroke (shift + ctrl + F), still in (F2) mode, click no full
	stroke style set 1.00 mm

Shift click on black on the bottom pallet
Save the SVG.


Follow this to convert svg
https://andrehessling.de/2016/09/13/getting-a-custom-board-outline-from-an-svg-file-into-kicad/
